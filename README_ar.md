# 🤖 مدونة التقنية بالذكاء الاصطناعي

> **منصة تدوين احترافية مدعومة بالذكاء الاصطناعي مع توليد محتوى متقدم**

منصة تدوين حديثة مبنية على Laravel 11 و Filament v4 تستفيد من الذكاء الاصطناعي لاكتشاف المواضيع التقنية الرائجة تلقائياً وتوليد مقالات تقنية عالية الجودة بلغات متعددة.

[![Laravel](https://img.shields.io/badge/Laravel-11.x-red.svg)](https://laravel.com)
[![Filament](https://img.shields.io/badge/Filament-v4.4-orange.svg)](https://filamentphp.com)
[![PHP](https://img.shields.io/badge/PHP-8.2+-blue.svg)](https://www.php.net)

---

## ✨ المميزات الرئيسية

### 🎯 توليد المحتوى بالذكاء الاصطناعي
- **دعم متعدد لمزودي الذكاء الاصطناعي**: التكامل مع Gemini و OpenAI و Claude و OpenRouter
- **اكتشاف الاتجاهات الآلي**: يقوم الذكاء الاصطناعي بمسح المشهد التقني للمواضيع الرائجة
- **توليد ذكي للمقالات**: ينشئ محتوى تقني احترافي محسّن لمحركات البحث
- **دعم متعدد اللغات**: توليد المقالات بالإنجليزية أو العربية
- **نظام قائمة المهام**: إدارة وتتبع مهام توليد المحتوى

### 🛠️ لوحة تحكم احترافية
- **إدارة مزودي الذكاء الاصطناعي**: تكوين وإدارة عدة مزودين
- **مركز التحكم في Pipeline**: مراقبة والتحكم في سير عمل توليد المقالات
- **إحصائيات فورية**: تتبع المهام المعلقة والجارية والمكتملة والفاشلة
- **تكوين مرن**: إعدادات قابلة للتخصيص للاتجاهات وعدد الكلمات والمزيد

### 📝 إدارة المحتوى
- **محرر نصوص متقدم**: تكامل TinyMCE متطور
- **نظام التصنيفات**: تنظيم المحتوى بكفاءة
- **تحسين محركات البحث**: علامات meta وتحسين مدمج
- **سير عمل المسودة/النشر**: مراجعة قبل النشر
- **تتبع المصادر**: تتبع مصادر المقالات وإلهاماتها

### 🔒 مميزات المؤسسات
- **مفاتيح API مشفرة**: تخزين آمن باستخدام تشفير Laravel
- **دعم متعدد المستخدمين**: التحكم في الوصول بناءً على الأدوار
- **تسجيل الأنشطة**: تتبع جميع عمليات الذكاء الاصطناعي
- **معالجة الأخطاء**: آليات إعادة محاولة قوية
- **معمارية قابلة للتوسع**: تصميم نظيف موجه للخدمات

---

## 🚀 البدء السريع

### المتطلبات الأساسية
- PHP 8.2 أو أحدث
- Composer
- Node.js و NPM
- SQLite (أو MySQL/PostgreSQL)

### التثبيت

```bash
# استنساخ المستودع
git clone https://github.com/yourusername/ai-tech-blog.git
cd ai-tech-blog

# تثبيت الاعتمادات
composer install
npm install

# إعداد البيئة
cp .env.example .env
php artisan key:generate

# تكوين قاعدة البيانات
touch database/database.sqlite

# تشغيل الترحيلات
php artisan migrate

# ملء مزودي الذكاء الاصطناعي (اختياري)
php artisan db:seed --class=AIProviderSeeder

# بناء الأصول
npm run build

# تشغيل الخادم
php artisan serve
```

قم بزيارة `http://localhost:8000/admin` للوصول إلى لوحة التحكم.

---

## ⚙️ التكوين

### 1. متغيرات البيئة

أضف إلى ملف `.env`:

```env
# Gemini AI (المزود الافتراضي)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# OpenAI (اختياري)
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4

# الإعدادات الافتراضية للـ Pipeline
AI_TRENDS_COUNT=5
AI_MIN_WORDS=800
AI_MAX_WORDS=1500
AI_CONTENT_LANGUAGE=en
```

### 2. إعداد مزود الذكاء الاصطناعي

#### عبر لوحة التحكم (موصى به)
1. انتقل إلى **Admin → AI System → AI Providers**
2. انقر **Create** واملأ:
   - الاسم: مثلاً "Gemini Pro"
   - النوع: اختر المزود (Gemini/OpenAI/إلخ)
   - مفتاح API: مفتاحك
   - النموذج: اسم النموذج
3. ضعه كـ **Active** و **Default**

#### عبر Seeder
```bash
php artisan db:seed --class=AIProviderSeeder
```

ينشئ:
- ✅ Gemini (نشط، افتراضي)
- OpenAI (مثال غير نشط)
- OpenRouter (مثال غير نشط)

---

## 📖 دليل الاستخدام

### توليد المقالات

#### الطريقة 1: لوحة التحكم (موصى به)

1. **انتقل إلى AI Pipeline Control**
   - اذهب إلى `Admin → AI Configuration → AI Pipeline Control`

2. **تكوين التوليد**
   - اختر مزود الذكاء الاصطناعي
   - اختر اللغة (إنجليزي/عربي)
   - حدد عدد الاتجاهات (1-10)
   - حدد عدد الكلمات المستهدف

3. **تنفيذ Pipeline**
   - انقر **Fetch New Trends** لاكتشاف المواضيع الرائجة
   - انقر **Process Pending Tasks** لتوليد المقالات
   - راقب التقدم في الوقت الفعلي

4. **المراجعة والنشر**
   - اذهب إلى **AI System → AI Tasks** لعرض المهام المولدة
   - انقر **View Post** لمراجعة المقالات
   - عدّل وانشر من قسم **Posts**

#### الطريقة 2: سطر الأوامر

```bash
# Pipeline كامل (جلب الاتجاهات + توليد المقالات)
php artisan ai:pipeline

# جلب الاتجاهات فقط
php artisan ai:pipeline --trends-only

# معالجة المهام الموجودة فقط
php artisan ai:pipeline --process-only

# تكوين مخصص
php artisan ai:pipeline --trends=3 --words=1000
```

---

## 🏗️ المعمارية

### التقنيات المستخدمة
```
الواجهة الأمامية:
├── Filament v4.4 (لوحة الإدارة)
├── TailwindCSS (التنسيق)
├── Livewire v3.7 (التفاعلية)
└── TinyMCE (محرر متقدم)

الواجهة الخلفية:
├── Laravel 11.x (الإطار)
├── SQLite/MySQL (قاعدة البيانات)
├── طبقة الخدمات (منطق العمل)
└── نمط المصنع (مزودو AI)

تكامل الذكاء الاصطناعي:
├── Google Gemini API
├── دعم OpenAI API
├── نظام مزودين قابل للتوسع
└── Prompting متعدد اللغات
```

### معمارية الخدمات

```
AIOrchestratorService (المتحكم الرئيسي)
    ├── AIProviderFactory (اختيار المزود)
    │   ├── GeminiTrendSearchService
    │   ├── GeminiContentGenerator
    │   ├── OpenAITrendSearchService (قابل للتوسع)
    │   └── OpenAIContentGenerator (قابل للتوسع)
    └── إدارة المهام
        ├── إنشاء المهام
        ├── معالجة المهام
        └── إعادة محاولة المهام الفاشلة
```

---

## 📊 مخطط قاعدة البيانات

### الجداول الأساسية

**`ai_providers`** - تكوينات خدمات الذكاء الاصطناعي
```
- id, name, type, api_key (مشفر)
- model, base_url, settings (json)
- is_active, is_default, priority
```

**`ai_tasks`** - قائمة مهام التوليد
```
- id, task_type, payload (json)
- status (pending/running/completed/failed)
- result (json), error_message
- scheduled_at, started_at, completed_at
```

**`pipeline_settings`** - التكوينات العامة
```
- key, value, type, group, description
```

**`posts`** - المقالات المولدة
```
- title, slug, content, category_id
- source_url, is_published, published_at
- featured_image, meta_description
```

---

## 🎨 مميزات لوحة التحكم

### 1. إدارة مزودي الذكاء الاصطناعي
- إضافة/تعديل/حذف المزودين
- اختبار اتصالات API
- تعيين المزود الافتراضي
- تكوين معاملات النموذج

### 2. التحكم في AI Pipeline
- **اختيار المزود**: اختر المزود النشط
- **اختيار اللغة**: إنجليزي أو عربي
- **تكوين الاتجاهات**: حدد العدد (1-10)
- **عدد الكلمات**: تخصيص طول المقال
- **إجراءات سريعة**: جلب الاتجاهات، معالجة المهام
- **إحصائيات مباشرة**: مراقبة المهام في الوقت الفعلي

### 3. مراقبة مهام الذكاء الاصطناعي
- عرض جميع مهام التوليد
- التصفية حسب الحالة/النوع
- إعادة محاولة المهام الفاشلة
- عرض المنشورات المولدة
- فحص تفصيلي للـ payload/النتيجة

### 4. إعدادات Pipeline
- عدد الاتجاهات الافتراضي
- عدد الكلمات الافتراضي
- تبديل النشر التلقائي
- إعدادات الصور المميزة

---

## 🔧 التخصيص

### إضافة مزود ذكاء اصطناعي جديد

1. **إنشاء فئات الخدمة**
```php
// app/Services/ClaudeTrendSearchService.php
class ClaudeTrendSearchService implements TrendSearchServiceInterface
{
    public function searchTrends(int $count = 5): array
    {
        // التنفيذ
    }
}

// app/Services/ClaudeContentGenerator.php
class ClaudeContentGenerator implements ContentGeneratorInterface
{
    public function generateArticle(array $trendData): array
    {
        // التنفيذ
    }
}
```

2. **تحديث المصنع**
```php
// app/Services/AIProviderFactory.php
public static function makeTrendSearch(AIProvider $provider)
{
    return match($provider->type) {
        'claude' => new ClaudeTrendSearchService($provider),
        // ...
    };
}
```

3. **إضافة إلى لوحة التحكم**
- أنشئ مزوداً في **AI Providers**
- اضبط النوع على 'claude'
- كوّن مفتاح API والنموذج

### تخصيص Prompts المقالات

عدّل `app/Services/GeminiContentGenerator.php`:
```php
private function buildArticlePrompt(array $trendData): string
{
    // تخصيص بنية الـ prompt
    // أضف متطلباتك
}
```

---

## 🐛 استكشاف الأخطاء وإصلاحها

### المشاكل الشائعة

**المشكلة: "API Key Invalid"**
```bash
# تحقق من مفتاح API في .env أو لوحة التحكم
# تأكد من أن المزود نشط
# اختبر الاتصال في AI Providers
```

**المشكلة: "Tasks Stuck in Pending"**
```bash
# معالجة المهام يدوياً
php artisan ai:pipeline --process-only

# أو عبر لوحة التحكم
AI Pipeline Control → Process Pending Tasks
```

**المشكلة: "Slug Duplicate Error"**
- تم إصلاحه تلقائياً مع توليد slug فريد
- المقالات تحصل على لواحق رقمية (-1, -2, إلخ)

---

## 🚀 النشر

### قائمة التحقق للإنتاج

```bash
# التحسين للإنتاج
composer install --optimize-autoloader --no-dev
php artisan config:cache
php artisan route:cache
php artisan view:cache
npm run build

# ضبط البيئة
APP_ENV=production
APP_DEBUG=false

# قاعدة البيانات
php artisan migrate --force

# Queue worker (موصى به)
php artisan queue:work --tries=3
```

### أمان البيئة
- ✅ تشفير مفاتيح API
- ✅ استخدام HTTPS
- ✅ تفعيل rate limiting
- ✅ نسخ احتياطية منتظمة
- ✅ مراقبة استخدام API

---

## 🤝 المساهمة

المساهمات مرحب بها! الرجاء:

1. Fork المستودع
2. إنشاء فرع للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح بموجب ترخيص MIT.

---

## 🙏 شكر وتقدير

- **فريق Laravel** - إطار عمل رائع
- **فريق Filament** - لوحة إدارة جميلة
- **Google Gemini** - واجهة برمجية قوية للذكاء الاصطناعي
- **OpenAI** - ابتكار في الذكاء الاصطناعي

---

## 📞 الدعم

- **التوثيق**: [التوثيق الكامل](AI_BLOGGING_PIPELINE.md)
- **المشاكل**: [GitHub Issues](https://github.com/yourusername/ai-tech-blog/issues)
- **البريد الإلكتروني**: support@example.com

---

**بُني بـ ❤️ باستخدام Laravel و Filament والذكاء الاصطناعي**

*آخر تحديث: يناير 2026*
